# Batiment Ruine

This repository is for the Batiment Ruine project. More details will be added soon!

## Import des adresses depuis Excel

Utiliser la commande suivante pour importer les adresses (`NomAr` -> `adresses.libelle`) :

```bash
php import_adresses.php
```
